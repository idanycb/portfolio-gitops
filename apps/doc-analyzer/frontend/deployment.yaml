apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: doc-analyzer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - envFrom:
            - configMapRef:
                name: env
          image: ghcr.io/idanycb/findoc-rag-frontend:main-1-14d0aa6d9de421ccb7030f49691ea8db1c97852b  # {"$imagepolicy": "doc-analyzer:frontend-policy"}
          name: frontend
          ports:
            - containerPort: 3000
              protocol: TCP
      restartPolicy: Always
